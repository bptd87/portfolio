import React from 'react';

export function BlogFormatter() {
  return (
    <div className="min-h-screen flex items-center justify-center p-8">
      <div className="max-w-3xl w-full bg-card border border-border p-8">
        <h1 className="text-2xl font-bold">Blog Formatter (placeholder)</h1>
        <p className="mt-4 text-muted-foreground">This is a lightweight placeholder component added so the dev server can run. Replace with the real `BlogFormatter` implementation from your design when available.</p>
      </div>
    </div>
  );
}

export default BlogFormatter;
